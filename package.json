{
  "name": "obsidian-color-master",
  "version": "1.0.6",
  "description": "Full control of Obsidian color CSS variables with profiles, import/export, bilingual UI, and Force Mode to override themes.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.js --bundle --platform=browser --format=cjs --outfile=main.js --minify --external:obsidian"
  },
  "keywords": [],
  "author": "Yazan Ammar",
  "license": "MIT",
  "dependencies": {
    "sortablejs": "^1.15.6"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
